#!/bin/bash

TT=$(which tt++)
TT_PATH="$HOME/.tintin"
if [[ ! -f $TT ]]; then
  echo 'tt++ not installed.'
  exit 1
fi

cd $TT_PATH
if [[ -n $1 ]]; then
  if [[ -f $1.tin ]]; then
    $TT $1.tin
  else
    echo "$TT_PATH/$1.tin is not available"
    exit 1
  fi
else
  $TT base.tin
fi
